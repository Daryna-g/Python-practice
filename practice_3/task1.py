text = input("Введіть довільний рядок: ")

if len(text) >= 15:
    # Отримуємо зріз від 12-го до 15-го символа (15-й не включається)
    slice_text = text[11:15]
    print("Зріз рядка:", slice_text)
else:
    print("Рядок занадто короткий для виконання зрізу.")
